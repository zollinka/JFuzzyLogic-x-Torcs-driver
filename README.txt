> Download Torcs -> http://torcs.sourceforge.net
> Directory with torcs should be in the same directory as EP.
> Create run_race file that runs wtorcs.exe with given driver and prints results.


wtorcs.exe sampling rate depends on the CPU -> try not to do many things during the optimalization
The optimalization methods in jFuzzyLogic are pretty random -> https://github.com/pcingola/jFuzzyLogic/tree/master/src/net/sourceforge/jFuzzyLogic/optimization
